{
  "name": "Clinical Diary - Developer (Default)",
  "dockerComposeFile": "../tools/dev-env/docker-compose.yml",
  "service": "dev",
  "workspaceFolder": "/workspace/src",

  "customizations": {
    "vscode": {
      "extensions": [
        // Flutter & Dart
        "Dart-Code.flutter",
        "Dart-Code.dart-code",

        // JavaScript/TypeScript
        "dbaeumer.vscode-eslint",
        "esbenp.prettier-vscode",

        // Python
        "ms-python.python",
        "ms-python.vscode-pylance",

        // Docker
        "ms-azuretools.vscode-docker",

        // Git
        "eamodio.gitlens",
        "github.vscode-pull-request-github",

        // GitHub Copilot (if available)
        "github.copilot",

        // General productivity
        "streetsidesoftware.code-spell-checker",
        "gruntfuggly.todo-tree",

        // Markdown
        "yzhang.markdown-all-in-one"
      ],

      "settings": {
        "terminal.integrated.defaultProfile.linux": "bash",
        "terminal.integrated.profiles.linux": {
          "bash": {
            "path": "/bin/bash",
            "args": ["-l"]
          }
        },

        // Editor settings
        "editor.formatOnSave": true,
        "editor.defaultFormatter": "esbenp.prettier-vscode",

        // Flutter settings
        "dart.flutterSdkPath": "/opt/flutter",

        // Role identification
        "window.title": "[DEV] ${dirty}${activeEditorShort}${separator}${rootName}"
      }
    },

    "codespaces": {
      "repositories": {
        "yourorg/*": {
          "permissions": {
            "contents": "write",
            "pull_requests": "write"
          }
        }
      }
    }
  },

  "features": {
    "ghcr.io/devcontainers/features/github-cli:1": {
      "version": "latest"
    }
  },

  "forwardPorts": [3000, 5000, 8080],

  "portsAttributes": {
    "3000": {
      "label": "Flutter Web",
      "onAutoForward": "notify"
    },
    "5000": {
      "label": "API Server",
      "onAutoForward": "notify"
    },
    "8080": {
      "label": "Preview",
      "onAutoForward": "openBrowser"
    }
  },

  "postCreateCommand": "cat /etc/motd && echo '' && echo '✅ Developer environment ready! Run: flutter --version'",

  "remoteUser": "ubuntu",

  "updateContentCommand": "git pull",

  "containerEnv": {
    "DOPPLER_PROJECT": "clinical-diary-dev",
    "DOPPLER_CONFIG": "dev"
  }
}
