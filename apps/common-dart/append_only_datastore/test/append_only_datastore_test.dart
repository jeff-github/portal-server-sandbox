import 'package:flutter_test/flutter_test.dart';

void main() {
  test('package can be imported', () {
    // Simple smoke test to ensure package is set up correctly
    // Real tests are in core_test.dart
    expect(1 + 1, equals(2));
  });
}
