#!/bin/bash
# =====================================================
# Git Hook: post-commit
# =====================================================
#
# This hook runs after successful commits.
#
# Plugins (in tools/claude-marketplace/):
# 1. anspar-workflow - Records commit in workflow state history
#
# To install this hook:
#   git config core.hooksPath .githooks
#
# =====================================================

# Note: This hook does not use set -e because we don't want
# failures to break the commit (which is already complete)

# =====================================================
# 1. Workflow State Tracking (Plugin)
# =====================================================

WORKFLOW_POSTCOMMIT_HOOK="tools/claude-marketplace/anspar-workflow/hooks/post-commit"
if [ -f "$WORKFLOW_POSTCOMMIT_HOOK" ]; then
    "$WORKFLOW_POSTCOMMIT_HOOK"
fi

# =====================================================
# Success (Always)
# =====================================================

exit 0
